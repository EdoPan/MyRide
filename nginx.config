http {
    upstream myride {
        least_conn;
        server 10.2.1.123:porta;
        server 10.2.1.124:porta;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://myride;
        }
    }
}