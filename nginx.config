http {
    upstream myride {
        least_conn;
        server 10.2.1.121:8080;
        server 10.2.1.122:8080;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://myride;
        }
    }
}